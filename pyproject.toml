[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "runbook-voice-assistant"
version = "0.1.0"
description = "Voice assistant that learns reusable runbooks from successful agent trajectories."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
voice = [
  "sounddevice>=0.5,<1",
]
sailbox = [
  "sail>=0.5.6,<1",
]
# The judge reaches Sail through its Anthropic-compatible Messages endpoint, so it
# needs the Anthropic SDK but not `sail` itself.
judge = [
  "anthropic>=0.40,<1",
]
# End-to-end parent workflow: Sailboxes for the children plus structured parent
# planning/judging through Sail's Anthropic-compatible endpoint.
live = [
  "sail>=0.5.6,<1",
  "anthropic>=0.40,<1",
]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  # Developers exercise the live voice loop, so dev installs include the
  # microphone/speaker adapter as well as the test tooling.
  "sounddevice>=0.5,<1",
  # schema/validate.py has always imported this; it was in no dependency list.
  "jsonschema>=4",
]

[project.scripts]
runbook-demo = "runbook_voice.demo:main"
runbook-branch-search-demo = "runbook_voice.branch_search_demo:main"
runbook-cold-task-demo = "runbook_voice.cold_task_demo:main"
runbook-distill = "runbook_voice.distiller:main"
runbook-judge-check = "runbook_voice.judge_check:main"
runbook-sailbox-demo = "runbook_voice.sailbox_demo:main"
runbook-voice-echo = "runbook_voice.voice_loop:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
asyncio_mode = "auto"

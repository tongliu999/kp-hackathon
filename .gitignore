node_modules/
.DS_Store
data/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.claude/settings.local.json
# Claude Code's own worktrees. Each is a git repo; committing one embeds a
# repo-inside-a-repo that no clone can resolve.
.claude/worktrees/

.env
.env.*

__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
dist/
build/
*.egg-info/
runbooks.json
.sailbox-demo.json
# runbook-demo runtime state: the store is rebuilt from demo/handwritten_runbook.json
# on every run, and bookings.json is live booking state, not source.
demo/runbook-store.json*
demo/bookings.json
demo/assistant-store.json*
demo/assistant-bookings.json
demo/fe-store.json*
demo/fe-bookings.json
demo/proof-store.json*
demo/proof-bookings.json
# Live branching-search output. fixtures/trajectories/ holds the locked
# hand-written examples; real runs must not be mixed into them.
runs/

# Session exports and vaults never belong in the tree. The vault itself refuses
# to be written here (src/vault/vault.js assertOutsideRepo); this is the second line.
*.enc.json
cookies*.json
cookies*.txt
storageState*.json
